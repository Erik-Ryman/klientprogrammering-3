import Nav from "@/components/Nav";
import React, { FC } from "react";

interface ChatProps {}

const Chat: FC<ChatProps> = ({}) => {
	return (
		<Nav>
			<div className='text-center'>The chat is not yet implemented</div>
		</Nav>
	);
};

export default Chat;
